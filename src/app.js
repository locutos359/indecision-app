console.log('App.js is running');

// JSX - JavaScript XML
const template = <h1>Does this change for realsies</h1>;
const appRoot = document.getElementById('app');
ReactDOM.render(template, appRoot);
